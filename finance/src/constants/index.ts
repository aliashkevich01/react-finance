
import data from '../data/schema.json';
export const FOOTER_NAV = ['Main', 'Contacts', 'About', 'License'];
export const SIGN_IN_INPUTS = ['mobilePhone', 'email', 'password'];
export const ALL_SEX = ['male', 'female']
export const parsedData = JSON.parse(JSON.stringify(data));